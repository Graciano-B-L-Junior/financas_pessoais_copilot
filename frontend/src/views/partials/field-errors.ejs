<% if (errors && errors[field] && errors[field].length) { %>
  <ul class="field-errors" id="<%= field %>-errors">
    <% errors[field].forEach(function(message) { %>
      <li><%= message %></li>
    <% }); %>
  </ul>
<% } %>