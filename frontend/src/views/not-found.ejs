<%- include('partials/head', { bodyClass: 'page--public', pageTitle }) %>
<%- include('partials/nav-public') %>

<main class="container page-shell">
  <section class="surface panel empty-state" style="padding: 2rem;">
    <span class="eyebrow">404</span>
    <h1 class="section-title">Pagina nao encontrada</h1>
    <p><%= message || 'O endereco solicitado nao existe ou foi removido.' %></p>
    <div class="hero-actions" style="justify-content: center;">
      <a class="btn btn--primary" href="/">Voltar para o inicio</a>
      <a class="btn btn--ghost" href="/dashboard">Ir para o dashboard</a>
    </div>
  </section>
</main>

<%- include('partials/footer') %>